version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.12
    commands:
      - pip install -r lambda/requirements.txt

  build:
    commands:
      - cp lambda/*.py package/

artifacts:
  base-directory: package
  files:
    - '**/*'